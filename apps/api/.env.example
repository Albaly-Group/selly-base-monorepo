# Backend API Environment Configuration

# Application
NODE_ENV=development
PORT=3001

# Database Configuration (Development Mode - Mock Data)
# Set SKIP_DATABASE=true to use mock data without requiring PostgreSQL
SKIP_DATABASE=true

# Database Configuration (Production Mode - PostgreSQL)
# When SKIP_DATABASE=false, configure one of the following:

# Option 1: Database URL (Recommended for cloud databases)
# DATABASE_URL=postgresql://user:password@localhost:5432/selly_base
# For cloud databases with SSL, add: ?sslmode=require or ?ssl=true

# Option 2: Individual database parameters
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# DATABASE_USERNAME=postgres
# DATABASE_PASSWORD=postgres
# DATABASE_NAME=selly_base

# JWT Configuration
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES_IN=1d

# CORS Configuration
# Comma-separated list of allowed origins (optional)
# CORS_ORIGINS=http://localhost:3000,https://yourdomain.com
