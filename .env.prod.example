# Production Environment Configuration for Docker Compose
# Copy this file to .env.prod and fill in your values

# ============================================================================
# DOMAIN CONFIGURATION
# ============================================================================
# Main domain for your application
DOMAIN=sellybase.albaly.jp

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
# PostgreSQL credentials
POSTGRES_USER=selly_user
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE
POSTGRES_DB=selly_base

# ============================================================================
# JWT AUTHENTICATION
# ============================================================================
# JWT Secret - MUST be a strong random string (generate with: openssl rand -base64 32)
JWT_SECRET=CHANGE_ME_GENERATE_STRONG_SECRET_HERE
# JWT Token expiration (e.g., 1d, 7d, 30d)
JWT_EXPIRES_IN=7d

# ============================================================================
# TRAEFIK DASHBOARD AUTHENTICATION
# ============================================================================
# Basic auth for Traefik dashboard (user:password)
# Generate with: echo $(htpasswd -nb admin your_password) | sed -e s/\\$/\\$\\$/g
# Default: admin:admin123 (CHANGE THIS!)
TRAEFIK_DASHBOARD_AUTH=admin:$$apr1$$8EVjn/nj$$GiLUZqcbueTFeD23SuB6x0

# ============================================================================
# SSL/TLS CERTIFICATE CONFIGURATION (Let's Encrypt)
# ============================================================================
# Choose ONE of the following methods:

# Method 1: TLS-ALPN-01 Challenge (Default, recommended for cloud)
# No additional configuration needed - works out of the box

# Method 2: HTTP-01 Challenge (Simple, requires port 80 accessible)
# Uncomment in traefik.yml if preferred

# Method 3: DNS-01 Challenge (Best for wildcard certificates)
# Choose your DNS provider:

# Option A: CloudFlare
# CF_API_EMAIL=your-cloudflare-email@example.com
# CF_DNS_API_TOKEN=your-cloudflare-api-token

# Option B: AWS Route53
# AWS_ACCESS_KEY_ID=your-aws-access-key-id
# AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
# AWS_REGION=us-east-1

# ============================================================================
# OPTIONAL: MONITORING & ALERTING
# ============================================================================
# If you plan to integrate monitoring tools
# PROMETHEUS_ENDPOINT=http://prometheus:9090
# GRAFANA_ENDPOINT=http://grafana:3000

# ============================================================================
# SECURITY NOTES
# ============================================================================
# 1. Always use strong, unique passwords
# 2. Never commit .env.prod to version control
# 3. Rotate secrets regularly
# 4. Use secret management services in production (AWS Secrets Manager, HashiCorp Vault)
# 5. Limit access to this file (chmod 600 .env.prod)
